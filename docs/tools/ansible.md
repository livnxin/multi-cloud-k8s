Ansible is used to ensure that each vm or server is configured in a reproducible way and to prevent increased maintaineance burden due to irreproducible configuration that necessitates expertise deriving from institutiional memory. By using a reproducible configuration as a code, the ansible configuration files become its own well documented institutional memory which will help in the process of onboarding new maintainers and other staff. 